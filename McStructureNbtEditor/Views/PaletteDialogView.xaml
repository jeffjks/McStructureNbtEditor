<Window x:Class="McStructureNbtEditor.Views.PaletteDialogView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:converters="clr-namespace:McStructureNbtEditor.ViewModels.Converters"
        mc:Ignorable="d"
        Title="{Binding Title}"
        Width="520"
        Height="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize">
    <Window.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </Window.Resources>
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Margin="0,0,0,0">
            <TextBlock Text="{Binding PaletteToEditText}"
                       Margin="0,0,0,4"
                       Visibility="{Binding ShowHeader, Converter={StaticResource BoolToVisibilityConverter}}"/>

            <Grid Margin="0,4,0,4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           VerticalAlignment="Center"
                           Text="{DynamicResource L_PaletteDialog_PaletteBlockName}"/>

                <TextBox Grid.Column="2"
                         MinWidth="220"
                         Height="24"
                         Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/>
            </Grid>
        </StackPanel>

        <TextBlock Grid.Row="2" Grid.Column="0"
                   Grid.ColumnSpan="3"
                   FontWeight="Bold"
                   Margin="0,4,0,4"
                   Text="Properties" />

        <DataGrid Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="3"
                  ItemsSource="{Binding Properties}"
                  SelectedItem="{Binding SelectedProperty}"
                  Margin="0,4,0,4"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  HeadersVisibility="Column"
                  SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Key"
                                    Binding="{Binding Key, UpdateSourceTrigger=PropertyChanged}"
                                    Width="*" />
                <DataGridTextColumn Header="Value"
                                    Binding="{Binding Value, UpdateSourceTrigger=PropertyChanged}"
                                    Width="*" />
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="3"
                    Margin="0,4,0,4"
                    Orientation="Horizontal">
            <Button Content="{DynamicResource L_PaletteDialog_AddProperty}"
                    Width="112"
                    Height="24"
                    Margin="0,0,8,0"
                    Command="{Binding AddPropertyCommand}" />
            <Button Content="{DynamicResource L_PaletteDialog_RemoveProperties}"
                    Width="112"
                    Height="24"
                    Command="{Binding RemovePropertyCommand}" />
        </StackPanel>

        <TextBlock Grid.Row="5" Grid.Column="0"
                   Grid.ColumnSpan="2"
                   VerticalAlignment="Center"
                   Foreground="IndianRed"
                   Text="{Binding ErrorMessage}" />

        <StackPanel Grid.Row="5" Grid.Column="2"
                    Margin="0,4,0,0"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="{DynamicResource L_Dialog_ButtonOk}"
                    Width="96"
                    Height="24"
                    Margin="0,0,8,0"
                    IsDefault="True"
                    Command="{Binding ConfirmCommand}" />
            <Button Content="{DynamicResource L_Dialog_ButtonCancel}"
                    Width="96"
                    Height="24"
                    IsCancel="True"
                    Command="{Binding CancelCommand}" />
        </StackPanel>
    </Grid>
</Window>