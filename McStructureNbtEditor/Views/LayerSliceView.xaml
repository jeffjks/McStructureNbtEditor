<UserControl x:Class="McStructureNbtEditor.Views.LayerSliceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <ScrollViewer HorizontalScrollBarVisibility="Auto"
                  VerticalScrollBarVisibility="Auto"
                  FocusVisualStyle="{x:Null}"
                  CanContentScroll="False">
        <Grid HorizontalAlignment="Center"
              VerticalAlignment="Center">
            <ItemsControl x:Name="CellItemsControl"
                          ItemsSource="{Binding SliceCells}"
                          PreviewMouseMove="CellItemsControl_PreviewMouseMove"
                          PreviewMouseLeftButtonUp="CellItemsControl_PreviewMouseLeftButtonUp">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="{Binding SliceWidth}"
                                     Rows="{Binding SliceHeight}" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Width="32"
                                Height="32"
                                Margin="0"
                                Padding="0"
                                Tag="{Binding}"
                                SnapsToDevicePixels="True"
                                Background="{Binding BlockName, Converter={StaticResource BlockToBrushConverter}}"
                                PreviewMouseLeftButtonDown="Cell_PreviewMouseLeftButtonDown">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="BorderBrush" Value="#FFAAAAAA" />
                                    <Setter Property="BorderThickness" Value="0.5" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                            <Setter Property="BorderBrush" Value="DodgerBlue" />
                                            <Setter Property="BorderThickness" Value="2" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>

                            <Grid>
                                <TextBlock Text="{Binding DisplayText}"
                                           FontSize="10"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           TextAlignment="Center"
                                           IsHitTestVisible="False" />
                            </Grid>

                            <Border.ToolTip>
                                <StackPanel>
                                    <TextBlock Text="{Binding TooltipBlockNameText, TargetNullValue=(empty)}" />
                                    <TextBlock Text="{Binding PaletteIndex, StringFormat=Palette: {0}}" />
                                    <TextBlock Text="{Binding X, StringFormat=X: {0}}" />
                                    <TextBlock Text="{Binding Y, StringFormat=Y: {0}}" />
                                    <TextBlock Text="{Binding Z, StringFormat=Z: {0}}" />
                                </StackPanel>
                            </Border.ToolTip>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
    </ScrollViewer>
</UserControl>